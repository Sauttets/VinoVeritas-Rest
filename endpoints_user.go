package main

import "github.com/gin-gonic/gin"

func newUser(c *gin.Context) {
	//username := c.Param("username")

	//TODO: generate new user in database and return key

}

func updateUser(c *gin.Context) {
	//key := c.Param("key")
	//username := c.Param("username")
}
